Module gets intent from text query using pywit 2.0

as argument for the application function expects dict structure with two fields:
	1) field action == 'listen' (same as before)
	2) field query - contains the query as string